spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sahtech
    username: root
    password: 
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
## LOGGING ##

logging:
  level:
    root: info
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %-5level- %logger{45} - %msg %n"

app:
  client:
    url: http://localhost:8080/*
twilio:
  phoneNumber: +13346001566
mailersend:
  fromEmail: nizaraoun1919@gmail.com
  fromName: Nizar Aoun

