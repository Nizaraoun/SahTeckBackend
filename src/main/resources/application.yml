spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: nizaraoun1919@gmail.com
    password: zzxt mvml lftu tlft
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  datasource:
    url: jdbc:mysql://sql6.freesqldatabase.com/sql6702920?useSSL=false
    username: sql6702920
    password: M4kwAldi1Y
    driver-class-name: com.mysql.cj.jdbc.Driver
  mvc:
    static-path-pattern: /uploads/**
  resources:
    static-locations: file:E:/PfeProject/BackendSahTech/SahTech/uploads/
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
  servlet:
    multipart:
      enabled: true
      max-file-size: 20MB
      max-request-size: 20MB

server:
  tomcat:
    max-swallow-size: 20MB

logging:
  level:
    root: info
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %-5level- %logger{45} - %msg %n"

app:
  client:
    url: http://localhost:8080/*
